## Excange rates

In the project directory, you can run:

* start propject:

### `npm install` or `npm -i`
### `npm start`

* build project:
### `npm run build`

* deployment:

### `npm run build` fails to minify